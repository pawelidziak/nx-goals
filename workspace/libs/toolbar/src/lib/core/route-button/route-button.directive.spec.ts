import { RouteButtonDirective } from './route-button.directive';

describe('RouteButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteButtonDirective();
    expect(directive).toBeTruthy();
  });
});
