import { ButtonPositionDirective } from './button-position.directive';

describe('ActionButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonPositionDirective();
    expect(directive).toBeTruthy();
  });
});
